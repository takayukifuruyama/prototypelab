# ロリポップサーバー設定ガイド

## 📌 概要
このドキュメントは、contact.htmlのお問い合わせフォームをロリポップサーバーで動作させるための設定手順です。

## ✅ ロリポップサーバーの対応状況
- ✅ PHP 7.4以上対応（すべてのプランで利用可能）
- ✅ mbstring拡張モジュール（標準で有効）
- ✅ メール送信機能（mb_send_mail使用可能）
- ✅ .htaccessファイル（利用可能）

## 🚀 セットアップ手順

### 1. ファイルのアップロード

#### FTPソフトを使う場合（推奨）

**FTP接続情報の確認**
1. ロリポップユーザー専用ページにログイン
2. 「ユーザー設定」→「アカウント情報」を開く
3. 以下の情報を確認:
   - FTPサーバー: `ftp.lolipop.jp`
   - FTPアカウント: `your-account`
   - FTPパスワード: （設定したパスワード）

**FileZillaでのアップロード手順**
1. FileZillaを起動
2. 接続情報を入力:
   - ホスト: `ftp.lolipop.jp`
   - ユーザー名: FTPアカウント
   - パスワード: FTPパスワード
   - ポート: 21
3. 接続後、以下のファイルをアップロード:
   ```
   / (ルートディレクトリ)
   ├── contact.html
   ├── send_mail.php
   ├── thanks.html
   ├── .htaccess（必要に応じて）
   ├── css/
   │   ├── contact.css
   │   └── thanks.css
   └── img/
       └── logo.png
   ```

#### ロリポップFTPを使う場合

1. ユーザー専用ページにログイン
2. 「サーバーの管理・設定」→「ロリポップ!FTP」を開く
3. ファイルをドラッグ&ドロップでアップロード

### 2. PHP設定の確認

ロリポップでは標準でPHP 7.4以上が使えますが、念のため確認します。

1. ユーザー専用ページ →「サーバーの管理・設定」→「PHP設定」
2. PHPバージョンが **7.4以上** になっているか確認
3. もし古い場合は、最新のバージョンに変更

### 3. ファイルパーミッションの設定

ロリポップFTPで以下のパーミッションに設定:

```
contact.html  → 604 または 644
send_mail.php → 604 または 644
thanks.html   → 604 または 644
css/          → 705 または 755
img/          → 705 または 755
```

**注意**: ロリポップでは`604`と`644`のどちらでも動作しますが、セキュリティ面では`604`が推奨されます。

### 4. メールアドレスの設定確認

`send_mail.php`の18行目を確認:

```php
$to = 'takayuki.f@itnav.co.jp';  // ← 受信先メールアドレス
```

このメールアドレスに問い合わせが届きます。

### 5. 動作確認

1. ブラウザでアクセス:
   ```
   https://your-domain.com/contact.html
   ```

2. フォームに入力して送信

3. 確認事項:
   - ✅ `thanks.html`にリダイレクトされる
   - ✅ 管理者宛（takayuki.f@itnav.co.jp）にメールが届く
   - ✅ 送信者宛に自動返信メールが届く

## ⚠️ ロリポップ特有の注意事項

### メール送信について

**1. メール送信数の制限**
- エコノミー: 100通/時間、1,000通/日
- ライト: 300通/時間、3,000通/日
- スタンダード以上: 1,000通/時間、10,000通/日

**2. 送信元メールアドレス**
自動返信メール（227行目）の送信元:
```php
$auto_reply_headers = "From: PROTOTYPE LAB <contact@itnav.co.jp>\n";
```

**推奨**: ロリポップのドメインメールアドレスを使用すると、迷惑メール扱いされにくくなります。

**3. SPFレコードの設定**
迷惑メール扱いを防ぐため、SPFレコードを設定することを推奨:

1. ムームードメインなど、ドメイン管理画面にログイン
2. DNSレコード設定を開く
3. 以下のTXTレコードを追加:
   ```
   v=spf1 include:lolipop.jp ~all
   ```

### セキュリティ対策

**1. .htaccessでのアクセス制限（オプション）**

必要に応じて`.htaccess`を作成し、以下を追加:

```apache
# PHPファイルへの直接アクセスを制限
<Files "send_mail.php">
    Order Deny,Allow
    Deny from all
    Allow from all
</Files>

# セキュリティヘッダー
<IfModule mod_headers.c>
    Header set X-Content-Type-Options "nosniff"
    Header set X-Frame-Options "DENY"
    Header set X-XSS-Protection "1; mode=block"
</IfModule>
```

**2. エラー表示の無効化**

本番環境では、`send_mail.php`の3-4行目が以下のようになっていることを確認:

```php
error_reporting(E_ALL);
ini_set('display_errors', 0);  // ← 0になっているか確認
```

## 🔧 トラブルシューティング

### 問題1: 500 Internal Server Error

**原因**: PHPの構文エラー、またはパーミッション設定が不適切

**対処法**:
1. エラーログを確認（ユーザー専用ページ →「ログ」→「エラーログ」）
2. ファイルパーミッションを確認（604または644）
3. PHPバージョンを確認（7.4以上）

### 問題2: メールが届かない

**原因1**: 迷惑メールフォルダに振り分けられている
- 対処: 迷惑メールフォルダを確認

**原因2**: メール送信制限に達している
- 対処: ユーザー専用ページでメール送信状況を確認

**原因3**: 送信元メールアドレスが不正
- 対処: `send_mail.php`の227行目の送信元を、ロリポップで設定したメールアドレスに変更

### 問題3: 文字化けする

**原因**: 文字コードの設定が間違っている

**対処法**:
1. ファイルがUTF-8で保存されているか確認
2. `send_mail.php`の187行目を確認:
   ```php
   mb_internal_encoding('UTF-8');
   ```

### 問題4: フォーム送信後に404エラー

**原因**: `send_mail.php`や`thanks.html`が正しくアップロードされていない

**対処法**:
1. FTPでファイルの存在を確認
2. ファイル名のスペルミスがないか確認
3. パーミッションが正しいか確認

## 📞 サポート

### ロリポップのサポートに問い合わせる場合

以下の情報を用意すると、スムーズに解決できます:
- エラーログの内容
- PHPバージョン
- プラン名（エコノミー、ライト、スタンダード等）
- 実行したい処理（メールフォームの設置）

### 参考リンク

- [ロリポップ！マニュアル - PHP](https://lolipop.jp/manual/user/php/)
- [ロリポップ！マニュアル - メール送信](https://lolipop.jp/manual/user/mail-send/)
- [ロリポップ！マニュアル - FTP](https://lolipop.jp/manual/user/ftp/)

## ✅ チェックリスト

設定完了前に、以下を確認してください:

- [ ] すべてのファイルがアップロードされている
- [ ] PHPバージョンが7.4以上になっている
- [ ] ファイルパーミッションが正しく設定されている
- [ ] `send_mail.php`の受信メールアドレスが正しい
- [ ] ブラウザでcontact.htmlにアクセスできる
- [ ] フォーム送信のテストが成功している
- [ ] メールが届く（管理者宛・自動返信）
- [ ] SPFレコードが設定されている（推奨）

---

**作成日**: 2026-01-14
**対象サーバー**: ロリポップ！レンタルサーバー
**PHPバージョン**: 7.4以上
